# Build resources

Иконки приложения для electron-builder:

- `icon.icns` — macOS (1024×1024)
- `icon.ico` — Windows (256×256 multi-res)
- `icon.png` — Linux (1024×1024)

При отсутствии electron-builder вставит дефолтную электронную иконку. Замените их на свои до релиза.
